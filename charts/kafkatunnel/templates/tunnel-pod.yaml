apiVersion: v1
kind: Pod
metadata:
  name: testclient
  namespace: {{ .Release.Namespace }}
spec:
  containers:
  - name: {{ .Values.global.modules.kafka.name }}
    image: {{ .Values.image }}:{{ .Values.imageTag }}
    command:
      - sh
      - -c
      - "exec tail -f /dev/null"