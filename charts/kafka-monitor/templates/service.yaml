apiVersion: v1
kind: Service
metadata:
  name: {{ template "kafka-monitor.fullname" . }}
  labels:
    app: {{ template "kafka-monitor.name" . }}
    chart: {{ template "kafka-monitor.chart" . }}
    release: {{ .Release.Name }}
    heritage: {{ .Release.Service }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    app: {{ template "kafka-monitor.name" . }}
    release: {{ .Release.Name }}
